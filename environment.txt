Environment
===========
 
Python version: Python 3.13.0
OS: Linux 6.6.97+ x86_64 GNU/Linux
Date run: 2026-08-24 to 2026-08-25
Platform: Udacity Workspace
 
Virtual environment setup (repeated per-project, once per system's solution/ directory)
-----------------------------------------------------------------------------------------
python -m venv .venv
.venv/bin/pip install -e ".[dev]"
source .venv/bin/activate
 
Notes
-----
- No ANTHROPIC_API_KEY was set in any environment; all extraction/pipeline runs used recorded/replay clients (--mode replay), and live-only tests were SKIPPED (see tests.txt for each system).
- The same three steps above were run independently inside each system's own solution/ directory (01-policy-pipeline, 02-mortgage-extraction, 03-supply-chain), each with its own pyproject.toml / .venv.
